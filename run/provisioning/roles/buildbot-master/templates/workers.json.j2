[
{% for worker_host in groups['buildbot-workers'] %}
  {
    "name": "{{hostvars[worker_host].application_user}}",
    "password": "{{hostvars[worker_host].buildbot_worker_password}}"
  }{% if not loop.last %},{% endif %}
{% endfor %}
]

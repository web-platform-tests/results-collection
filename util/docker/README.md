# Docker utility files

## dev.sh

Run the file-watching, auto-compiling dev server in a Docker instance under the
name `wptd-dev-instance`.

## travis_(step).sh

Docker-based script for completing `(step)` in Travis CI.

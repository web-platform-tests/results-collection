# The Python modules listed here facilitate local development. Application
# dependencies are expressed with separate "requirements" files within the
# configuration management scripts.
pycodestyle==2.4.0
requests==2.21.0

matrix:
  include:
    - language: node_js
      node_js:
        - "node"

    - language: python
      python: 3.4
      install: "pip install -r requirements.txt"
      script:
        - python -m unittest discover -p "*_test.py"
